diff --git a/kernel/proc.c b/kernel/proc.c
--- a/kernel/proc.c
+++ b/kernel/proc.c
@@ -1,3 +1,4 @@
+// Sandbox fields: sandbox_on/sandbox_action/allow_mask[4]
@@
   // Initialize signal handling
   p->pending_signals = 0;
   for(int i = 0; i < NSIG; i++) {
     p->sig_handlers[i] = (uint64)SIG_DFL;
   }
+
+  // Initialize sandbox (seccomp-lite)
+  p->sandbox_on = 0;
+  p->sandbox_action = 0;
+  memset(p->allow_mask, 0, sizeof(p->allow_mask));
@@
   // Initialize signal handling
   p->pending_signals = 0;
   for(int i = 0; i < NSIG; i++) {
     p->sig_handlers[i] = (uint64)SIG_DFL;
   }
+
+  // Clear sandbox state
+  p->sandbox_on = 0;
+  p->sandbox_action = 0;
+  memset(p->allow_mask, 0, sizeof(p->allow_mask));
@@
-  // copy trace mask
-  np->tmask = p->tmask;
+  // copy trace mask
+  np->tmask = p->tmask;
+
+  // inherit sandbox policy (recommended)
+  np->sandbox_on = p->sandbox_on;
+  np->sandbox_action = p->sandbox_action;
+  memmove(np->allow_mask, p->allow_mask, sizeof(np->allow_mask));
